#include<stdio.h>
#include<conio.h>
main()
{
	float desp, gorg;
	printf("Informe a despesa: ");
	scanf("%f", &desp);
	gorg=desp*0.10;
	printf("A gorgeta para o garcom sera: %f", gorg);
	getch();
	
}
